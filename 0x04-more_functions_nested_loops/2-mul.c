#include "holberton.h"
/**
 * mul - Function to multiply two variables
 *
 * @a,@b: Variables to test
 *
 * Return: Product of the two variables.
 */
int mul(int a, int b)
{
_putchar('%d', (a * b));
return (c);
}
